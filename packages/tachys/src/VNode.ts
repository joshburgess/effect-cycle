import type { VNode as TachysVNode } from "tachys/sync"

/**
 * A virtual DOM node for the tachys renderer: an instance of tachys's
 * `VNode` class produced by the `h(...)` JSX factory (or the JSX runtime).
 *
 * @since 0.1.0
 */
export type VNode = TachysVNode
