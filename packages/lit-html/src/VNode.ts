import type { TemplateResult } from "lit-html"

/**
 * A virtual DOM node for the lit-html renderer: a `TemplateResult` produced
 * by lit-html's `html` (or `svg`) tagged template literal.
 *
 * @since 0.1.0
 */
export type VNode = TemplateResult
