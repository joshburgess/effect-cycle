import type { VNode as VueVNode } from "vue"

/**
 * A virtual DOM node for the Vue 3 renderer: a Vue `VNode` produced by
 * the `h(...)` factory or by templates compiled to render functions.
 *
 * @since 0.1.0
 */
export type VNode = VueVNode
